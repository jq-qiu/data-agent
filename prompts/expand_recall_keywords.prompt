你是一名数据分析语义规划专家。

请根据【用户问题】和【已有关键词】，一次性生成三类用于召回的关键词：

1. column_keywords
- 用于检索数据库字段。
- 仅包含回答问题必需的字段概念，例如：地区、商品、销售额、订单日期。
- 不得包含具体字段取值、表名或 SQL。

2. metric_keywords
- 用于检索业务指标。
- 包含指标概念、常见同义词或缩写，例如：销售额、GMV、订单量、客单价。
- 不得包含具体字段取值、表名或 SQL。

3. value_keywords
- 只能包含可能真实存在于数据库字段中的取值，例如：SP、圣保罗州、信用卡、床浴用品。
- 禁止包含“统计”“排名”“前三”“销售额”等操作词、排序词或指标名。
- 问题中没有明确字段取值时，返回空数组。

要求：
- 关键词保持简短、去重，不输出完整问句。
- 仅输出合法 JSON，不要输出 Markdown 或解释。

【用户问题】
{query}

【已有关键词】
{keywords}

输出格式：
{{
  "column_keywords": [],
  "metric_keywords": [],
  "value_keywords": []
}}
